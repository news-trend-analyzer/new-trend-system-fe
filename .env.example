# API Base URLs
# 프로덕션 환경에서 사용할 API 서버 URL을 설정합니다
VITE_API_BASE_URL=http://localhost:3002
VITE_SEARCH_API_BASE_URL=http://localhost:3001

# 개발 환경에서는 vite.config.ts의 proxy 설정을 사용합니다
# 프로덕션 환경에서는 위의 환경 변수를 설정해야 합니다
